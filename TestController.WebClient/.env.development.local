# Local dev override (mode-specific, highest precedence; gitignored by convention).
# Points the Vite dev proxy at the standalone TestController.WebApi instead of the
# WPF-embedded host on 5200. Delete this file to revert to the .env.development default.
VITE_DEV_PROXY_TARGET=http://127.0.0.1:5050
