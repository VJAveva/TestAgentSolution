<UserControl x:Class="TestControllerGrpc.Views.AgentWorkspace.FleetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:TestControllerGrpc.ViewModels.AgentWorkspace">

    <DockPanel>
        <!-- Summary bar -->
        <Border DockPanel.Dock="Top" Padding="10,6">
            <DockPanel>
                <!-- Register Agent button -->
                <Button DockPanel.Dock="Right" Margin="8,0,0,0" Padding="10,4"
                        FontSize="10" Cursor="Hand"
                        Command="{Binding RequestRegisterAgentCommand}"
                        Background="{DynamicResource AccentBtnBg}"
                        Foreground="{DynamicResource Accent}"
                        BorderBrush="{DynamicResource Accent}"
                        BorderThickness="1">
                    <TextBlock Text="+ Register Agent" FontWeight="SemiBold"/>
                </Button>
                <!-- Filter textbox -->
                <TextBox DockPanel.Dock="Right" Width="140" Margin="8,0,0,0"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Background="{DynamicResource BgCard}"
                         Foreground="{DynamicResource TextP}"
                         BorderBrush="{DynamicResource Bdr}"
                         FontSize="10" Padding="4,3" VerticalContentAlignment="Center"/>
                <!-- Stats with utilization bar -->
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" FontSize="11" Foreground="{DynamicResource TextS}">
                        <Run Text="{Binding TotalCount, Mode=OneWay}" FontWeight="SemiBold" Foreground="{DynamicResource TextP}"/>
                        <Run Text=" agents ·"/>
                        <Run Text="{Binding BusyCount, Mode=OneWay}" Foreground="{DynamicResource Accent}"/>
                        <Run Text=" busy ·"/>
                        <Run Text="{Binding FreeCount, Mode=OneWay}" Foreground="{DynamicResource AccGreen}"/>
                        <Run Text=" free ·"/>
                        <Run Text="{Binding FailedCount, Mode=OneWay}" Foreground="{DynamicResource AccRed}"/>
                        <Run Text=" failed ·"/>
                        <Run Text="{Binding OfflineCount, Mode=OneWay}" Foreground="{DynamicResource AccRed}"/>
                        <Run Text=" offline"/>
                    </TextBlock>
                    <!-- Fleet utilization bar -->
                    <Border Width="80" Height="4" CornerRadius="2" Margin="10,0,0,0"
                            Background="{DynamicResource Bdr}" VerticalAlignment="Center"
                            ToolTip="Fleet utilization (busy/total)">
                        <Border HorizontalAlignment="Left" Height="4" CornerRadius="2"
                                Background="{DynamicResource Accent}"
                                Width="{Binding UtilizationBarWidth, Mode=OneWay}"/>
                    </Border>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Grouped agent panel -->
        <Grid>
            <!-- Empty state -->
            <TextBlock Text="No agents registered"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="12" Foreground="{DynamicResource TextS}"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolVis}}"/>

            <!-- Grouped list -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="6"
                          Visibility="{Binding IsEmpty, Converter={StaticResource InvBoolVis}}">
                <ItemsControl ItemsSource="{Binding Groups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:FleetGroupVM}">
                            <StackPanel Margin="0,0,0,4">

                                <!-- GROUP HEADER -->
                                <Border Background="{DynamicResource BgPanel}"
                                        BorderBrush="{DynamicResource Bdr}"
                                        BorderThickness="0,0,0,0.5"
                                        Padding="12,7">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Group status dot -->
                                        <Ellipse Grid.Column="0" Width="8" Height="8"
                                                 Margin="0,0,8,0" VerticalAlignment="Center">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Fill" Value="{DynamicResource Accent}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding AccentKind}" Value="Idle">
                                                            <Setter Property="Fill" Value="{DynamicResource AccGreen}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>

                                        <!-- Title + Owner -->
                                        <StackPanel Grid.Column="1" Orientation="Horizontal"
                                                    VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}" FontSize="12"
                                                       FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextP}"/>
                                            <TextBlock Text="{Binding OwnerDisplay}" FontSize="10"
                                                       Margin="6,0,0,0"
                                                       Foreground="{DynamicResource TextS}"/>
                                        </StackPanel>

                                        <!-- Agent count -->
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Count}" FontSize="11"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextS}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- CARD GRID (2-column UniformGrid) -->
                                <ItemsControl ItemsSource="{Binding Agents}" Margin="6,4,6,6"
                                              Visibility="{Binding HasAgents, Converter={StaticResource BoolVis}}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="2"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:FleetCardVM}">
                                            <Border Width="Auto" Height="50" Margin="3" CornerRadius="5"
                                                    Padding="7,5" Cursor="Hand"
                                                    MouseLeftButtonUp="Card_Click">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource StatusSuccessBg}"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource StatusSuccessFg}"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                        <Setter Property="Opacity" Value="0.65"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Busy">
                                                                <Setter Property="Background" Value="{StaticResource StatusRunningBg}"/>
                                                                <Setter Property="BorderBrush" Value="{StaticResource StatusRunningFg}"/>
                                                                <Setter Property="Opacity" Value="1.0"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Failed">
                                                                <Setter Property="Background" Value="{StaticResource StatusFailedBg}"/>
                                                                <Setter Property="BorderBrush" Value="{StaticResource StatusFailedFg}"/>
                                                                <Setter Property="Opacity" Value="1.0"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Offline">
                                                                <Setter Property="Background" Value="{StaticResource StatusFailedBg}"/>
                                                                <Setter Property="BorderBrush" Value="{StaticResource StatusFailedFg}"/>
                                                                <Setter Property="Opacity" Value="0.7"/>
                                                            </DataTrigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="BorderBrush" Value="{DynamicResource AccPeach}"/>
                                                                <Setter Property="Opacity" Value="1.0"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>
                                                    <!-- Row 1: dot + agent name -->
                                                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                                                        <Ellipse Width="7" Height="7" Margin="0,0,5,0"
                                                                 VerticalAlignment="Center">
                                                            <Ellipse.Style>
                                                                <Style TargetType="Ellipse">
                                                                    <Setter Property="Fill" Value="{DynamicResource AccGreen}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Status}" Value="Busy">
                                                                            <Setter Property="Fill" Value="{DynamicResource Accent}"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Status}" Value="Failed">
                                                                            <Setter Property="Fill" Value="{DynamicResource AccRed}"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Status}" Value="Offline">
                                                                            <Setter Property="Fill" Value="{DynamicResource AccRed}"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Ellipse.Style>
                                                        </Ellipse>
                                                        <TextBlock Text="{Binding AgentName}" FontSize="11"
                                                                   FontWeight="SemiBold" FontFamily="Consolas"
                                                                   Foreground="{DynamicResource TextP}"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   MaxWidth="120"/>
                                                        <!-- Pipeline tag pill (visible when busy) -->
                                                        <Border Margin="6,0,0,0" CornerRadius="3"
                                                                Padding="5,1" VerticalAlignment="Center"
                                                                Background="{DynamicResource AccentBtnBg}"
                                                                BorderBrush="{DynamicResource Accent}"
                                                                BorderThickness="0.5">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Status}" Value="Busy">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                            <TextBlock FontSize="9"
                                                                       Foreground="{DynamicResource Accent}"
                                                                       TextTrimming="CharacterEllipsis"
                                                                       MaxWidth="160"
                                                                       Text="{Binding PipelinePillText}"/>
                                                        </Border>
                                                    </StackPanel>
                                                    <!-- Row 2: status detail -->
                                                    <TextBlock Grid.Row="1" Text="{Binding StatusDetail}"
                                                               FontSize="9" FontFamily="Consolas"
                                                               Foreground="{DynamicResource TextS}"
                                                               VerticalAlignment="Bottom"
                                                               TextTrimming="CharacterEllipsis"
                                                               Margin="12,2,0,0"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </DockPanel>
</UserControl>
