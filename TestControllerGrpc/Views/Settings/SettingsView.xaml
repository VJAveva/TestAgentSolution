<UserControl x:Class="TestControllerGrpc.Views.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TestControllerGrpc.Views.Settings">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel MaxWidth="800">
            <TextBlock Text="Settings" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,24"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>

            <!-- Security Mode Panel -->
            <local:SecurityModePanel />
        </StackPanel>
    </ScrollViewer>
</UserControl>
