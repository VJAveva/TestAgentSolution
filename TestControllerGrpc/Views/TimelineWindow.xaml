<Window
    x:Class="TestControllerGrpc.Views.TimelineWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:exec="clr-namespace:TestControllerGrpc.ViewModels.Execution"
    Title="Timeline View - Execution Dashboard"
    Height="600" Width="1200" MinWidth="600" MinHeight="300"
    WindowStartupLocation="CenterScreen"
    Background="{StaticResource DashboardBg}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Stats bar (shared header) -->
        <UniformGrid Grid.Row="0" Rows="1" Margin="8">
            <Border Background="{StaticResource CardBg}" CornerRadius="4" Padding="8" Margin="2">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="{Binding Dashboard.ActiveSessionCount}" FontSize="18" FontWeight="Medium"
                               Foreground="{StaticResource StatusRunningFg}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Active sessions" FontSize="10"
                               Foreground="{StaticResource TextDim}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBg}" CornerRadius="4" Padding="8" Margin="2">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="{Binding Dashboard.LockedAgentCount}" FontSize="18" FontWeight="Medium"
                               Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Agents locked" FontSize="10"
                               Foreground="{StaticResource TextDim}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBg}" CornerRadius="4" Padding="8" Margin="2">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="{Binding Dashboard.TotalPassedActions}" FontSize="18" FontWeight="Medium"
                               Foreground="{StaticResource StatusSuccessFg}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Actions passed" FontSize="10"
                               Foreground="{StaticResource TextDim}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBg}" CornerRadius="4" Padding="8" Margin="2">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="{Binding Dashboard.TotalFailedActions}" FontSize="18" FontWeight="Medium"
                               Foreground="{StaticResource StatusFailedFg}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Actions failed" FontSize="10"
                               Foreground="{StaticResource TextDim}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBg}" CornerRadius="4" Padding="8" Margin="2">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock FontSize="18" FontWeight="Medium"
                               Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center">
                        <Run Text="{Binding Dashboard.OverallProgressPercent, Mode=OneWay}"/><Run Text="%"/>
                    </TextBlock>
                    <TextBlock Text="Overall progress" FontSize="10"
                               Foreground="{StaticResource TextDim}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Time axis header -->
        <Canvas Grid.Row="1" x:Name="TimeAxis" Height="24" Margin="130,0,8,0"
                Background="{StaticResource CardBg}" ClipToBounds="True"/>

        <!-- Gantt body -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Timeline.Lanes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type exec:TimelineAgentLane}">
                        <Grid Height="40" Margin="0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <!-- Agent label -->
                            <Border Grid.Column="0" Background="{StaticResource AgentColumnBg}"
                                    BorderBrush="{StaticResource RowBorder}" BorderThickness="0,0,0.5,0"
                                    Padding="8,0">
                                <TextBlock Text="{Binding AgentName}" FontFamily="Consolas" FontSize="11"
                                           FontWeight="Medium" Foreground="{StaticResource TextPrimary}"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <!-- Action bars canvas -->
                            <Canvas Grid.Column="1" x:Name="BarCanvas" Background="Transparent"
                                    SizeChanged="BarCanvas_SizeChanged" ClipToBounds="True">
                                <ItemsControl ItemsSource="{Binding Bars}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <Canvas/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter">
                                            <Setter Property="Canvas.Top" Value="4"/>
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type exec:TimelineBar}">
                                            <Border Height="28" CornerRadius="4" Padding="4,0"
                                                    ToolTip="{Binding Tooltip}" Cursor="Hand">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource StatusIdleBg}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Running">
                                                                <Setter Property="Background" Value="{StaticResource StatusRunningBg}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Success">
                                                                <Setter Property="Background" Value="{StaticResource StatusSuccessBg}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Failed">
                                                                <Setter Property="Background" Value="{StaticResource StatusFailedBg}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding ActionTag}" FontSize="10"
                                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Running">
                                                                    <Setter Property="Foreground" Value="{StaticResource StatusRunningFg}"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Success">
                                                                    <Setter Property="Foreground" Value="{StaticResource StatusSuccessFg}"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Failed">
                                                                    <Setter Property="Foreground" Value="{StaticResource StatusFailedFg}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- Time cursor line -->
                                <Line Y1="0" Y2="40" Stroke="{StaticResource StatusRunningFg}"
                                      StrokeThickness="1" StrokeDashArray="4 2" Opacity="0.6"
                                      x:Name="CursorLine"/>
                            </Canvas>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
